version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 12
    commands:
      - echo Executing Install Phase
  pre_build:
    commands:
      - npm install
  build:
    commands:
      - npm run build:ci


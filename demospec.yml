version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 12
    commands:
      - echo Executing Install Phase
  pre_build:
    commands:
      - npm install
  build:
    commands:
      - npm run build:ci

artifacts:
  files:
    - '**/*.html'
    - '**/*.css'
    - '**/*.js'
    - '**/*.ico'
  base-directory: 'dist/popular-consensus'
  disgard-paths: yes
